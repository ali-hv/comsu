Suggest 5 commit messages using only the format "type: message", where type is:
- `feat`: new features
- `fix`: bug fixes
- `chore`: maintenance
- `style`: code style
- `ref`: refactoring
- `perf`: performance
- `docs`: documentation
- `build`: build/dependencies
- `ci/cd`: CI/CD changes

Please strictly follow the `type` guidelines based on the description of the changes,
and return only the commit messages without additional explanations and don't return my changes to me again just the commit message.
Format each message on a new line with NO introduction text or additional explanations.
Just the commit messages:
type: message
type: message
type: message
type: message
type: message

Changes: